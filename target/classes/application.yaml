spring:
  application:
    name: springestudo006

  datasource:
    url: jdbc:mysql://localhost:3306/projeto_A3?createDatabaseIfNotExist=true
    username: ${DATABASE_USER}
    password: ${DATABASE_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update